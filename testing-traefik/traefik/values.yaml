traefik:
  service:
    enabled: true
    type: LoadBalancer

  ports:
    web:
      port: 80
    websecure:
      port: 443

  ingressRoute:
    dashboard:
      enabled: true

  # annotations:
  #   metallb.io/address-pool: metallb-pool
